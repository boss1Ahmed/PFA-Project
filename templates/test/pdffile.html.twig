<html>
<head>
    <style>
        table, th, td {
            border: 1px solid black;
        }
    </style>
</head>
<body>

<table style="width: 100%;" class="header">
    <tr>
        <td><h1 style="text-align: left">{{ tache.technicien.nom }} {{ tache.technicien.prenom }}</h1></td>
        <td><h1 style="text-align: right">{{ date|date('Y-m-d à H-i') }}</h1></td>
    </tr>
</table>

<table style="width: 100%; font-size: 8pt;">
    <tr>
        <td>Machine: <strong>{{ tache.intervention.machine.nomMachine }}</strong></td>
        <td>Intervention: <strong>{{ tache.intervention.id }}</strong></td>
    </tr>
</table>
<table style="width: 100%">
    <thead>
    <tr>
        <th>nom piece</th>
        <th>quantite</th>
    </tr>
    </thead>
    <tbody>
    {% for piece in tache.pieces %}
        <tr>
            <td>{{ piece.pieceRechange.libelle}}</td>
            <td>{{ piece.quantite }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>